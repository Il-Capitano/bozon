import std::libc::stdio;

export function print_stdout(s: str)
{
	libc::fwrite(s.begin_ptr(), 1, s.size(), libc::stdout());
}

export function print_stderr(s: str)
{
	libc::fwrite(s.begin_ptr(), 1, s.size(), libc::stderr());
}
