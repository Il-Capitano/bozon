function check(intN: typename, r)
{
	let _ = __builtin_integer_range_from_end_iterator(r);
}

function main()
{
	let begin = 3i8;
	let r = __builtin_integer_range_from_i8(begin);
	check(int8, r);
	let begin = -33i8;
	let r = __builtin_integer_range_from_i8(begin);
	check(int8, r);
	let begin = 0i8;
	let r = __builtin_integer_range_from_i8(begin);
	check(int8, r);

	let begin = 3i16;
	let r = __builtin_integer_range_from_i16(begin);
	check(int16, r);
	let begin = -33i16;
	let r = __builtin_integer_range_from_i16(begin);
	check(int16, r);
	let begin = 0i16;
	let r = __builtin_integer_range_from_i16(begin);
	check(int16, r);

	let begin = 3i32;
	let r = __builtin_integer_range_from_i32(begin);
	check(int32, r);
	let begin = -33i32;
	let r = __builtin_integer_range_from_i32(begin);
	check(int32, r);
	let begin = 0i32;
	let r = __builtin_integer_range_from_i32(begin);
	check(int32, r);

	let begin = 3i64;
	let r = __builtin_integer_range_from_i64(begin);
	check(int64, r);
	let begin = -33i64;
	let r = __builtin_integer_range_from_i64(begin);
	check(int64, r);
	let begin = 0i64;
	let r = __builtin_integer_range_from_i64(begin);
	check(int64, r);

	let begin = 3u8;
	let r = __builtin_integer_range_from_u8(begin);
	check(uint8, r);
	let begin = 0u8;
	let r = __builtin_integer_range_from_u8(begin);
	check(uint8, r);

	let begin = 3u16;
	let r = __builtin_integer_range_from_u16(begin);
	check(uint16, r);
	let begin = 0u16;
	let r = __builtin_integer_range_from_u16(begin);
	check(uint16, r);

	let begin = 3u32;
	let r = __builtin_integer_range_from_u32(begin);
	check(uint32, r);
	let begin = 0u32;
	let r = __builtin_integer_range_from_u32(begin);
	check(uint32, r);

	let begin = 3u64;
	let r = __builtin_integer_range_from_u64(begin);
	check(uint64, r);
	let begin = 0u64;
	let r = __builtin_integer_range_from_u64(begin);
	check(uint64, r);
}
