import counting_resource;

function test(a: bool, b: bool, result: bool)
{
	let a_and_b = (counting_resource_t(), a) && (counting_resource_t(), b);
	if (a_and_b != result) unreachable;
}

function main()
{
	test(false, false, false);
	if (!counting_resource_is_valid()) unreachable;

	test(true, false, false);
	if (!counting_resource_is_valid()) unreachable;

	test(false, true, false);
	if (!counting_resource_is_valid()) unreachable;

	test(true, true, true);
	if (!counting_resource_is_valid()) unreachable;
}
