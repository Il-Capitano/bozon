// error: invalid memory access of an object of type 'i32'
// note: in call to 'function bar(p: *i32) -> void'
// note: while evaluating expression at compile time
// note: address points to an object from a finished stack frame
// error: failed to evaluate expression at compile time
function foo() -> *i32
{
	let i = 0;
	return &i;
}

function bar(p: *i32)
{
	*p;
}

function test()
{
	(consteval {
		bar(foo());
	});
}
