// warning: taking 'sizeof' of an expression with reference type '&i32' results in the size of type 'i32' [-W sizeof-reference-expression]
function main()
{
	let i = 0i32;
	let &r = i;

	static_assert(sizeof r == sizeof i32);
}
